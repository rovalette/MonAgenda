<Window x:Class="MyWPFAgenda.EPLieu"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="EPLieu" 
        Height="95"
        Width="300"
        WindowStyle="ToolWindow"
        WindowStartupLocation="CenterScreen"
        SizeToContent="WidthAndHeight"
        MaxHeight="525"
        MinHeight="130"
        MaxWidth="600"
        MinWidth="300"
        ResizeMode="NoResize">
    <StackPanel x:Name="bigpanel" Margin="0,0,0,0">
        <TextBlock x:Name="title" Text="Evenements par lieux" Background="blue" TextAlignment="Center" MaxHeight="70" MinHeight="70" VerticalAlignment="Top" Foreground="White" FontSize="30"/>
        <ComboBox Name="combo" SelectionChanged="changeIndex" />
        <TextBox x:Name="zoneText" VerticalScrollBarVisibility="Auto" MaxHeight="395"/>
    </StackPanel>
</Window>
